npm nai command
npm nai translate -> just translates contents of {baseLanguage}.json to {language}.json for all languages
npm nai dev -> watch version of run
npm nai run -> scans files, extracts strings, rewrites components and runs all translations
npm nai extract -> extracts strings, writes them to {baseLanguage}.json
npm nai rewrite -> rewrites components where necessary based on contents of {baseLanguage}.json